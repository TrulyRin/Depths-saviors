# Pages

## /dashboard

Entry: `dashboard.html`

Dependencies:

- `dashboard.html`
  - `dashboard.css`
  - `dashboard.js`
  - `style.css`

The rendered dashboard has login, server selection, and settings shell states. Public pilot configuration must appear only after the authenticated server selector returns an allowlisted guild.
