# Shared layout

## Dashboard shell

- Source: `dashboard.html`
- Description: fixed site navigation, three dashboard views (login, server picker, server settings), left feature sidebar, and toast surface.

The page uses a `nav.navbar`, `.dash-app`, `#view-login`, `#view-servers`, and `#view-dashboard` containing `.dash-sidebar` plus `.dash-content`.
